
<h1>Take Two!</h1>

<% if user_signed_in? %>
<%= link_to 'Dashboard', dashboard_index_path %>
<%= link_to "Create new listing", new_product_path%>
 <% end %>
 <br><br>
<ul>
<%= @products.each do |product| %>
  <%= link_to product.title %> 
  <%# <h5><%= link_to @product.title, product_path(product) %>
  <%= link_to "View", product_path(product) %> |
   <%# <%= if user_signed_in? && product.user_id == current_user  %> 
  <%= link_to "Edit", edit_product_path(product) %> |
  <%= link_to "Delete", product_path(product), method: :delete, data:{
    confirm: "Are you sure you want to delete this listing?"} %><br><br>
  <% end %>
  </ul>
  